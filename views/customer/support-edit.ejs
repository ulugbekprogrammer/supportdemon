<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Support Request - Dern-Support</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <%- include('../partials/header') %>
  
  <main class="support-edit-page">
    <div class="container">
      <div class="page-header">
        <h1>Edit Support Request</h1>
        <a href="/customer/support/<%= supportRequest._id %>" class="btn btn-secondary btn-sm">Back to Request</a>
      </div>
      
      <% if (typeof error !== 'undefined') { %>
        <div class="alert alert-error">
          <%= error %>
        </div>
      <% } %>
      
      <div class="form-container">
        <form action="/customer/support/<%= supportRequest._id %>" method="POST" class="support-form">
          <div class="form-section">
            <h2>Request Details</h2>
            
            <div class="form-group">
              <label for="title">Title</label>
              <input 
                type="text" 
                id="title" 
                name="title" 
                required 
                value="<%= supportRequest.title %>"
              >
            </div>
            
            <div class="form-group">
              <label for="description">Description</label>
              <textarea 
                id="description" 
                name="description" 
                rows="6" 
                required
              ><%= supportRequest.description %></textarea>
            </div>
            
            <div class="form-row">
              <div class="form-group">
                <label for="deviceType">Device Type</label>
                <select id="deviceType" name="deviceType" required>
                  <option value="desktop" <%= supportRequest.deviceType === 'desktop' ? 'selected' : '' %>>Desktop Computer</option>
                  <option value="laptop" <%= supportRequest.deviceType === 'laptop' ? 'selected' : '' %>>Laptop</option>
                  <option value="tablet" <%= supportRequest.deviceType === 'tablet' ? 'selected' : '' %>>Tablet</option>
                  <option value="smartphone" <%= supportRequest.deviceType === 'smartphone' ? 'selected' : '' %>>Smartphone</option>
                  <option value="printer" <%= supportRequest.deviceType === 'printer' ? 'selected' : '' %>>Printer</option>
                  <option value="server" <%= supportRequest.deviceType === 'server' ? 'selected' : '' %>>Server</option>
                  <option value="network" <%= supportRequest.deviceType === 'network' ? 'selected' : '' %>>Network Equipment</option>
                  <option value="other" <%= supportRequest.deviceType === 'other' ? 'selected' : '' %>>Other</option>
                </select>
              </div>
              
              <div class="form-group">
                <label for="urgency">Urgency</label>
                <select id="urgency" name="urgency" required>
                  <option value="low" <%= supportRequest.urgency === 'low' ? 'selected' : '' %>>Low - Not urgent</option>
                  <option value="medium" <%= supportRequest.urgency === 'medium' ? 'selected' : '' %>>Medium - Needs attention soon</option>
                  <option value="high" <%= supportRequest.urgency === 'high' ? 'selected' : '' %>>High - Urgent issue</option>
                  <option value="critical" <%= supportRequest.urgency === 'critical' ? 'selected' : '' %>>Critical - Emergency</option>
                </select>
              </div>
            </div>
            
            <div class="form-group">
              <label for="location">Preferred Service Location</label>
              <select id="location" name="location">
                <option value="">No preference</option>
                <option value="onsite" <%= supportRequest.location === 'onsite' ? 'selected' : '' %>>On-site (at your location)</option>
                <option value="office" <%= supportRequest.location === 'office' ? 'selected' : '' %>>At our office</option>
                <option value="remote" <%= supportRequest.location === 'remote' ? 'selected' : '' %>>Remote assistance</option>
              </select>
            </div>
          </div>
          
          <div class="form-actions">
            <button type="submit" class="btn btn-primary">Update Support Request</button>
            <a href="/customer/support/<%= supportRequest._id %>" class="btn btn-secondary">Cancel</a>
          </div>
        </form>
      </div>
    </div>
  </main>
  
  <%- include('../partials/footer') %>
  
  <script src="/js/main.js"></script>
</body>
</html>
